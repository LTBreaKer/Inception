server	{
	listen 80;

	root /var/www/mysite;

	index index.html;
	# server_name aharrass.42.fr;
	# ssl_protocols TLSv1.2 TLSv1.3;

	location /	{
		try_files $uri $uri/ = 404;
		index index.html;
	}
}
server {
    listen 8080;

    root /var/www/mysite;
    index index.html;

    location / {
        try_files $uri $uri/ =404;
    }
}